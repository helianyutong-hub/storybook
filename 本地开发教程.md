# Storybook 本地开发教程

> 从截图整理，方便本地查阅

---

## 一、怎么把代码弄到本地

1. 点上方下载卡片，把 `storybook-source.tar.gz` 下载到电脑上。
2. 解压到你想放的位置（比如 `D:\Storybook` 或 `~/storybook`），解压后目录是这样的：

```
storybook/
├── frontend/          # 前端项目：React + Vite
├── backend/           # 后端服务：Express，含 mock 数据
├── .cli-workspace/    # 云配置，基本不用管，留着就行
├── start-frontend.sh  # 启动前端脚本（必读）
└── README.md
```

> 好啦！这个后端不需要装 API 密钥（当前用免费的 Edge TTS），故事本跑起来后，打开网页就能用。

---

## 二、本地运行前准备

只需要两样东西：

1. **Node.js 18 以上**（推荐 20 LTS）→ https://nodejs.org 下载安装
2. **pnpm**：装完 Node 后在命令行执行：

```bash
npm install -g pnpm
```

搞定：`node -v` 和 `pnpm -v` 都能出版本号就完成了。

---

## 三、安装依赖（只需第一次）

打开终端，分别进两个目录各装依赖：

```bash
cd storybook/frontend
pnpm install

cd ../backend
pnpm install
```

---

## 四、启动（开两个命令行窗口）

### 窗口 1 — 后端（保持开着）

```bash
cd storybook/backend
pnpm dev
```

看到 `3000` 端口就成功了。

### 窗口 2 — 前端（保持开着）

```bash
cd storybook/frontend
pnpm dev
```

看到 `5173` 端口就成功了。

然后在浏览器打开：**http://localhost:5173** 就能看到故事了，跟在微信里一样好用。

> 你之后只要改代码就会自动刷新。数据在 `backend/data/db.json`，语音缓存存在 `backend/data/tts/`。

---

## 五、本次改好的 5 个功能（已落地）

| 需求 | 改了什么 | 涉及文件 |
|------|----------|----------|
| 1. 完整有情节的故事（像白雪公主） | 接入可选大模型生成完整情节；没配密钥时自动回退本地模板。需要你提供大模型 API Key（见第六节） | 新增 `backend/src/modules/storyGen.ts`；改了 `frontend/src/lib/api.ts`、`frontend/src/lib/storyEngine.ts`、`frontend/src/pages/Generating.tsx` |
| 2. 微信打开不用试听也能出声 | 进入播放页自动开始播放；微信拦截自动播放时，轻触屏幕任意位置即解锁音频 | `frontend/src/pages/Player.tsx` |
| 3. 一次试听自动翻页听完所有页 | 预览页新增「自动播放全部（自动翻页）」按钮，顺序播放整本、可随时停止 | `frontend/src/pages/Preview.tsx` |
| 4. 页数随情节自动生成 | 用大模型时，页数由情节长度决定（不再限定 4/6/8 页），插画按每页场景自动匹配 | `storyEngine.ts`（`buildStoryFromLLM`）+ 后端 `/story-gen` |
| 5. 故事全文展示区 + 重新生成文案 | 预览页新增「故事全文」区块，列出所有页文案便于核对；区块内和顶部都有「重新生成文案」按钮 | `frontend/src/pages/Preview.tsx` |

---

## 六、让故事有完整情节：需要你配合的（大模型 API）

要让故事像白雪公主那样有完整的前因后果，需要接一个大模型。代码我已经接好了，**你只需要在后端填一个 API Key 就能打开**：

1. 进 `backend/` 目录，复制环境变量模板：

```bash
cp .env.example .env
```

2. 用记事本打开 `backend/.env`，把下面几行改成你自己的（任选一家 OpenAI 格式的都行：OpenAI / DeepSeek / 通义 / 智谱 等）：

```bash
LLM_API_KEY=sk-你的key
LLM_BASE_URL=https://api.openai.com/v1        # DeepSeek 填 https://api.deepseek.com/v1
LLM_MODEL=gpt-4o-mini                          # 或 deepseek-chat 等
```

3. 重启后端（关掉后端窗口重开 `pnpm dev`），回到前端重新生成故事即可。

> 不填也没关系：代码会自动回退到本地模板引擎，故事照样能生成、能播放，只是情节是模板拼接的、偏简单。

---

## 七、本地改完，怎么回到微信里的新线上链接

有两种方式，本次改的代码**不用自己部署到服务器**：

1. **文档类发**：你本地改好后，把代码打包成 `tar.gz`，压缩后在上传，帮你新发布到新的链接。
2. **自己的**：`raa6da251558be29.app.arkid.weixin.link`（打开会 `https` 多余域名），自己托管。

> 本次改进已把"调用大模型生成完整剧情"真正接好（后端 `story-gen` 接口 + 前端生成流程），不再只是伪脚本——你配好 `LLM_API_KEY` 就能直接出效果。

