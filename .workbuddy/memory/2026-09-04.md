# 2026-09-04 工作日志

## 语音合成切换到阿里云 CosyVoice（音色区分问题）

**背景**：用户反馈「爸爸/妈妈/爷爷/奶奶」四个音色区分不明显，要求先改阿里云语音合成；
并明确「做不到区分就砍掉音色选择，能做到就保留」。

**结论：能明显区分，音色选择保留。**

### 音色映射（backend/src/lib/tts.ts 的 ALIYUN_VOICE）

| 角色 | 音色 | voice 参数 | 特征 | 年龄 |
|---|---|---|---|---|
| 宝妈 | 龙婉君 | longwanjun_v3 | 细腻柔声女 | 20-30 |
| 宝爸 | 龙安昀 | longanyun_v3 | 居家暖男 | 30-35 |
| 爷爷 | 龙老伯 | longlaobo_v3 | 沧桑岁月爷 | 60+ |
| 奶奶 | 龙老姨 | longlaoyi_v3 | 烟火从容阿姨 | 60+ |

原本 Edge TTS 只能靠调 pitch，四个音色几乎一样。换成不同年龄+性别的真实音色后区分度很高。
音色列表官方文档：https://help.aliyun.com/zh/model-studio/cosyvoice-voice-list

### 关键实现点

- **双端点 × 双音色 = 4 种降级组合**：
  端点①`https://{WorkspaceId}.cn-beijing.maas.aliyuncs.com/api/v1/services/audio/tts/SpeechSynthesizer`
  （voice 放在 `input` 里）；端点②`https://dashscope.aliyuncs.com/api/v1/services/aigc/multimodal-generation/generation`
  （voice 放在 `parameters` 里）。配了 WorkspaceId 就优先走①。
  每个音色还配了 fallback，主音色不可用自动降级。
- **401/403 直接放弃阿里云**（换端点换音色都没用），回退 Edge TTS。
- **缓存 key 带引擎名** `${ttsProvider()}:${lang}:${voice}:${text}`，切换服务商后旧音频自动失效。
- **官方已知问题处理**：cosyvoice-v3-flash 遇「·」会漏读 → 替换为中文逗号；文本截断 2000 字。
- **自检端点** `GET /api/tts/provider` 返回当前引擎、是否已配置 Key、每个音色对应关系。
- 成本：1 元/万字符，免费额度 1 万字符（90 天）。一个故事约 800 字 ≈ 0.08 元。

### 验证

- 后端 `tsc --noEmit` 通过
- 未配 Key 时 `/api/tts/provider` 返回 `provider: edge`，语音生成正常
- 配**假 Key** 时自检显示 `provider: aliyun`，生成语音时成功回退 Edge 返回 URL（容错链路 OK）

### 踩坑记录（重要）

1. **同一文件的多个 Edit 不要并行发** —— 并行时后发的编辑会基于同一快照覆盖，
   导致部分改动丢失（本次丢了 2 处 COSYVOICE_MODEL 替换 + 1 个路由）。必须串行。
2. **切分支部署前必须先在源码分支提交完所有新文件** —— 本次为了省事先 `git stash` 再切 gh-pages，
   结果 `git add -A` 把未跟踪的新文档一起提交，之后 `git reset --hard origin/gh-pages` 把它删了，
   文档白写一遍。正确顺序：main 提交 → 推送 → 再切 gh-pages 部署。
3. **npm 装依赖用国内镜像** —— 官方源 ECONNRESET 跑了 7 分钟还失败，
   换 `--registry=https://registry.npmmirror.com` 后 25 秒装完。

### vite base 改为环境变量控制

`base: process.env.VITE_BASE_PATH || '/'`
- 阿里云同源部署：默认根路径 `/`
- GitHub Pages：构建时设 `VITE_BASE_PATH=/storybook/`

之前把 base 硬改成 `/` 导致部署到 gh-pages 子路径会 404，这样两种部署都兼容了。

### 提交

- main `ba68e4bd`：阿里云 TTS 改造全部源码
- gh-pages `e3080ce2`：前端构建产物（音色特征提示已上线）
- 新增 `deploy/阿里云语音合成配置.md`（开通步骤、配置、验证、费用、排障）
